# The npm_package_* environment variables are automatically generated by npm
# based on the values specified in your project's package.json file
# (for example $npm_package_name, $npm_package_version).
VITE_PROJECT_VERSION=$npm_package_version

VITE_API_BASE_URL=https://example.com